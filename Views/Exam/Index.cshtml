@{
    ViewData["Title"] = "Итоговый экзамен";
}

<div class="container mx-auto max-w-3xl px-4 py-12">
    <div class="bg-white rounded-2xl shadow-2xl p-10 relative overflow-hidden">
        <div class="absolute -top-8 right-8 opacity-10 text-9xl select-none pointer-events-none">🎓</div>
        <h1 class="text-4xl font-extrabold mb-2 text-center text-purple-700 flex items-center justify-center gap-2">
            <svg class="w-10 h-10 text-emerald-500 inline-block" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 14l9-5-9-5-9 5 9 5zm0 0v6m0 0H6m6 0h6" />
            </svg>
            Итоговый экзамен
        </h1>
        <p class="text-lg text-gray-700 mb-6 text-center">Проверьте свои знания и получите именной сертификат!</p>
        @if (ViewBag.Message != null)
        {
            <div class="mb-8 p-5 bg-yellow-50 border-l-4 border-yellow-400 rounded-lg text-yellow-900 text-center text-lg font-semibold shadow">
                <svg class="w-7 h-7 inline-block mr-2 align-middle text-yellow-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M12 8v.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                </svg>
                @ViewBag.Message
            </div>
        }
        else
        {
            <div class="mb-6 flex flex-col sm:flex-row sm:space-x-8 space-y-2 sm:space-y-0">
                <div class="flex-1 bg-emerald-50 rounded-lg p-4 text-center border border-emerald-200">
                    <div class="text-sm text-gray-500 mb-1">Вопросов</div>
                    <div class="text-2xl font-bold text-emerald-700">20</div>
                </div>
                <div class="flex-1 bg-purple-50 rounded-lg p-4 text-center border border-purple-200">
                    <div class="text-sm text-gray-500 mb-1">Время</div>
                    <div class="text-2xl font-bold text-purple-700">15 мин</div>
                </div>
                <div class="flex-1 bg-yellow-50 rounded-lg p-4 text-center border border-yellow-200">
                    <div class="text-sm text-gray-500 mb-1">Сертификат</div>
                    <div class="text-2xl font-bold text-yellow-600">100% верно</div>
                </div>
            </div>
            <div class="mb-6 p-4 bg-emerald-50 border border-emerald-200 rounded-lg text-emerald-900 text-center text-base">
                <span class="font-semibold">Условия:</span> Ответьте правильно на все вопросы, чтобы получить сертификат. Вопросы выбираются случайно из всех тем курса.
            </div>
            <div class="mb-8 text-center text-gray-500 text-sm italic">
                Удачи! Ты справишься — и сертификат будет твоим 🎉
            </div>
            <div class="flex justify-center">
                <a href="@Url.Action("Start", "Exam")" class="inline-flex items-center justify-center px-8 py-4 border border-transparent text-lg font-bold rounded-xl text-white bg-emerald-600 hover:bg-emerald-700 transition-colors duration-300 shadow-lg">
                    Начать экзамен
                </a>
            </div>
        }
    </div>
</div> 